{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install --workspace=packages/database --workspace=apps/backend --legacy-peer-deps && cd packages/database && npx prisma generate && cd ../apps/backend && npm run build"
  },
  "deploy": {
    "preDeployCommand": "cd packages/database && npx prisma migrate deploy",
    "startCommand": "node apps/backend/dist/main",
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
